version: "3.8"

services:
  chroma:
    image: ghcr.io/chroma-core/chroma:latest
    container_name: chroma
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      CHROMA_SERVER_ALLOW_HTTP: "true"  # allow REST API over HTTP
